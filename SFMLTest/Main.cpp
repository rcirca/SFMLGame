#include "stdafx.h"
#include "Game.h"

int main()
{
	Game::Start();
	//sf::RenderWindow window(sf::VideoMode(200, 200), "Fucken HELLO WORLD!!");

	//while (window.isOpen())
	//{
	//	sf::Event event;
	//	while (window.pollEvent(event))
	//	{
	//		if (event.type == sf::Event::Closed)
	//			window.close();
	//	}
	//	
	//	window.clear();
	//	window.display();
	//}

	return 0;
}